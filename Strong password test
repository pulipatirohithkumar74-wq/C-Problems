#include <stdio.h>
#include <string.h>

int main()
{
    char pass[100];

    scanf("%s", pass);

    if(strlen(pass) >= 8)
        printf("Strong Password");
    else
        printf("Weak Password");

    return 0;
}
