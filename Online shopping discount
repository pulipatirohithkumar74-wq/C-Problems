#include <stdio.h>

int main()
{
    float amount;

    scanf("%f",&amount);

    if(amount >= 5000)
        amount = amount - (amount * 0.20);

    else if(amount >= 2000)
        amount = amount - (amount * 0.10);

    printf("Final Price = %.2f", amount);

    return 0;
}
