#include <stdio.h>

int main()
{
    int balance = 5000, withdraw;

    scanf("%d",&withdraw);

    if(withdraw <= balance)
    {
        balance -= withdraw;
        printf("Remaining Balance = %d", balance);
    }
    else
    {
        printf("Insufficient Balance");
    }

    return 0;
}
