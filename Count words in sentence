#include <stdio.h>

int main()
{
    char str[100];
    int i=0, count=1;

    fgets(str, sizeof(str), stdin);

    while(str[i]!='\0')
    {
        if(str[i]==' ' && str[i+1]!=' ')
            count++;
        i++;
    }

    printf("%d",count);

    return 0;
}
